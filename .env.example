# Turso Database Configuration (Optional - for future database features)
# Get these values from: https://turso.tech/
TURSO_DATABASE_URL="libsql://your-database-name.turso.io"
TURSO_AUTH_TOKEN="your-turso-auth-token-here"

# IMPORTANT:
# Current implementation uses hardcoded articles (no database required)
# Only configure if you plan to use database features in the future

# FOR CLOUDFLARE PAGES:
# D1 database binding is automatic via wrangler.toml
# No environment variables needed for D1
