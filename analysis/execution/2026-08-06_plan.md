# SLockHub SEO Execution Plan

- Date: 2026-08-06
- Inputs reviewed:
  - `analysis/seo-rules.md`
  - `analysis/gsc/2026-08-06_weekly.md`
  - `analysis/tech/2026-08-06_diagnosis.md`
  - `analysis/content/2026-08-06_growth.md`
- Operating rule: no backlinks or outreach work.
- Protected list: do not auto-change verified canonical tags, live 301/302 redirects, or live structured data fields.
- Architecture principle: consolidate repeated SEO surfaces into shared source-of-truth modules instead of page-by-page patches.

## Executive Decision

The weekly GSC movement remains Decision B: suspected algorithm/query-mix impact, monitor without escalation. The implementation below does not claim a confirmed technical incident. It resolves source-level architecture and content-trust gaps that were independently found by the technical and content workflows.

## Requirement Coverage Matrix

| Requirement / Finding | Source | Status | Implementation / Decision | Protected List |
|---|---|---|---|---|
| Preserve protected canonical, redirect, and schema fields | `seo-rules.md` | Done | No canonical tag output, redirect rule, or schema generator was changed. Compare URL cleanup uses internal links, static params, sitemap URL selection, and a runtime 404 gate for non-canonical reverse slugs. Security FAQ copy in `registry.ts` was aligned with article claims; current article JSON-LD does not emit `article.faqs` except the separate hardcoded Zigbee page schema path. | Guarded |
| Human sitemap incomplete and exposed admin/API links | Tech diagnosis | Done | `app/sitemap/page.tsx` now uses shared calculator, priority best, protocol, comparison, and core-hub registries; it lists all 32 calculators and omits admin/API links. | No |
| XML sitemap drops dynamic SEO URLs when DB fetch fails | Tech diagnosis / follow-up crawl | Done on 2026-08-07 | `app/sitemap.ts` now uses shared calculator slugs plus static fallback URLs for priority comparisons, priority brands, priority best pages, and protocol pages; dynamic DB results are de-duplicated over fallback entries when available, with one internal URL per unordered comparison pair. | No |
| Calculator related links were client/API dependent and weak for raw HTML crawlers | Tech diagnosis | Done | Calculator related links now flow through shared static `calculatorLinksMap`; old duplicate layout injection was removed; old `/api/related-articles` route and its DB-only helpers were removed. | No |
| Missing `/llms.txt` | Tech diagnosis / GEO | Done | Added `public/llms.txt` with primary hubs, priority tools, guides, comparison pages, evidence policy, and site info. | No |
| Article trust signal needs visible author/source boundary | Content audit | Done | `ArticleHeader` now renders a visible byline with fallback `SLockHub Editorial Team`. | No |
| Door compatibility pillar too thin for its GSC exposure | Content audit | Done for first pass | Expanded `door-compatibility-guide` into a measurement/fit hub; synced MDX frontmatter, `registry.ts`, and generated article content. | No |
| Security page overclaimed evidence scope | Content audit | Done for first pass | Retitled as Practical 2026 Threat Model; removed "200+ CVEs / tested encryption" claim from registry metadata and MDX-facing output; added scope/evidence wording and synced updated date. | No |
| Long-tail compare pages need query-specific packaging, including content query gaps for Schlage/Weiser, Tedee/Nuki, Lockly/Schlage, and Eufy/SimpliSafe | Tech + content audits | Done for first pass on 2026-08-07 | Added shared `priorityComparisonLinks`, expanded pair SEO profiles with choose reasons, door/protocol/price/region/model caveats, evidence boundaries, related comparison links, compare hub links, brand-page links, and human sitemap links. Added shared comparison URL canonicalization for internal links/static params/sitemap/runtime route handling so priority pairs do not expose duplicate directions. | Guarded; no canonical tag, redirect, or schema fields changed |
| Core pages lack proof of >=50 relevant internal links | Tech diagnosis | First-pass crawl recovery implemented; topical authority not proven | Added shared research-hub links through `SeoPathways` and recorded a rendered `<main>` crawl in `analysis/tech/2026-08-07_internal-link-crawl.md`. `/calculators`, `/protocols`, `/resources`, the security article, and the long-battery best page now exceed the crawl-recovery inbound threshold, but this is mostly from sitewide Research Hubs and must not be treated as final topical-authority proof before retest. | No |
| Best pages need visible ranking methodology and evidence layer | Content audit | Done for first pass on 2026-08-07 | Added shared best-page evidence boundaries with last-reviewed date, inclusion rule, exclusion rule, source boundary, and data limitations; rendered in the shared best-page template for Matter, Z-Wave, HomeKit, battery-life, and all profile-backed best pages. | Guarded; Product/ItemList schema fields unchanged |
| Protocol/integration authority coverage is uneven, including `zigbee smart lock` weakness, `/articles/protocols/connect-lock-to-homekit`, and HomeKit support opportunity | Content audit | Limited existing-page hardening on 2026-08-07; broader expansion gated | Updated five existing thin support pages with decision matrices, operating scenarios, failure handling, and validation checklists. No new pages were created. Existing Zigbee/HomeKit pages, including `/articles/protocols/connect-lock-to-homekit`, remain in the 2026-09-17 retest bucket, and broader protocol/integration expansion stays gated until GSC retest confirms demand and avoids dilution. | No |
| Potential overlap between Z-Wave intent pages | Content audit | Recommendation only | Batch 4 manual review. No merge, canonical, or redirect action without approval. | Protected if consolidation is needed |
| Compare hub visible priority links and CollectionPage schema parity | Website-seo final review | Gated recommendation | `/compare` now exposes priority comparison links in visible content. Its CollectionPage ItemList schema remains unchanged because structured-data edits are protected and require explicit approval. | Protected |

## Execution Batches

### Batch 1 - Implemented

Owner model: main Codex + read-only Next.js verification agent.

Changes:

- Consolidated calculator related resources into static mapped internal links.
- Removed the stale dynamic `/api/related-articles` surface after confirming no callers remained.
- Rebuilt the human sitemap from shared route registries instead of a hand-maintained partial list.
- Added XML sitemap fallback entries for high-value compare, brand, best, protocol, and calculator URLs when dynamic DB reads fail.
- Added `/llms.txt` for AI crawler guidance.
- Added visible article byline.
- Expanded door compatibility into the receiving hub for measurement, retrofit, install-cost, and protocol planning.
- Reframed the security flagship so metadata, title, body, and evidence scope match.

Verification already run:

- `npm run articles:generate`
- `npx tsx tests/seo-content-consistency.test.ts`
- `npm run lint`
- `npm run next:build`
- `npm run build`
- Raw HTML check on local production server:
  - `/calculators/compatibility` contains related article/tool links and does not contain `/api/related-articles`.
  - `/articles/security/smart-lock-security-complete-analysis` contains the new title, updated date, byline, and no old CVE/test claim.
  - `/sitemap` has 32 unique calculator links and no admin/API href.
  - `/llms.txt` has priority guides and no admin/API references.

### Batch 2 - Compare Pages And Silo Counts

Implementation update on 2026-08-07:

- Added shared `lib/seo/priority-comparisons.ts` as the source of truth for priority comparison links and one canonical internal URL per unordered brand pair.
- Added pair-specific choose reasons, caveats, and evidence boundaries to `lib/seo/comparison-page-seo.ts`.
- Updated `/compare/[slug]` to render the pair-specific decision layer and related comparison Silo links.
- Updated `/compare`, brand detail pages, static compare params, XML sitemap, and the human sitemap to use the shared priority comparison source.
- Updated `/compare/[slug]` so reverse-direction duplicate slugs return 404 instead of rendering a second self-canonical comparison page.
- Verified the previously unreachable explicit entries `/compare/samsung-vs-xiaomi`, `/compare/tedee-vs-august`, and `/compare/veise-vs-schlage` in rendered HTML.
- Protected canonical, redirect, and JSON-LD schema fields were not changed.
- A source-scan regression test now checks static `/compare/*-vs-*` links against the shared canonical comparison helper.
- Rendered `<main>` crawl proof now confirms all monitored core and priority compare URLs exceed the first-pass crawl-recovery inbound-link threshold; see `analysis/tech/2026-08-07_internal-link-crawl.md`. This remains a source-type heuristic and needs GSC/crawl retest before being considered topical-authority proof.

Recommended agents:

- Agent A: relevance engineering / compare-intent reviewer.
- Agent B: architecture/internal-link Silo reviewer.

Scope:

- `/compare/schlage-vs-weiser`
- `/compare/schlage-vs-defiant`
- `/compare/kwikset-vs-defiant`
- `/compare/nuki-vs-tedee`
- `/compare/lockly-vs-schlage`
- `/compare/eufy-vs-simplisafe`
- affected GSC pages from weekly monitoring.

Deliverables:

- Pair-specific above-fold verdict per page.
- "Choose X if / choose Y if" blocks.
- Door-fit, protocol, price, region, and model-level caveats.
- Relevant inbound-link report with core pages measured against the >=50 crawl-recovery target and caveated separately from topical-authority proof.
- No canonical/redirect/schema changes unless separately approved.

Retest:

- First GSC retest: 2026-09-03.
- Technical crawl/link retest: 2026-08-27.

### Batch 3 - Best, Protocol, And Integration Authority

Implementation update on 2026-08-07:

- Added shared `getBestPageEvidence()` to provide last-reviewed, inclusion, exclusion, source-boundary, and data-limitation copy.
- Updated the shared `/best/[slug]` template to render the evidence boundary before product shortlists.
- Verified Matter, Z-Wave, and HomeKit best pages in rendered HTML.
- Protected Product/ItemList schema fields were not changed.
- Added a limited existing-page hardening pass for five thin protocol/integration support pages. This does not open the gate for broad new-page expansion; it only improves pages already in the site architecture.
- Remaining: new protocol/integration pages and larger cluster expansion are still gated until first-pass updates are remeasured.

Recommended agents:

- Agent A: E-E-A-T / content trust reviewer.
- Agent B: topic authority / Silo planner.

Scope:

- `/best/matter-smart-locks`
- `/best/z-wave-smart-locks`
- `/best/homekit-smart-locks`
- `/best/smart-locks-with-longest-battery-life`
- Existing Zigbee and ecosystem support pages.
- Existing integration cluster pages, especially thin enterprise/automation/supporting pages.

Deliverables:

- Methodology blocks with inclusion/exclusion criteria.
- Last-verified dates and product-data limitations.
- Links from best pages to protocol, compatibility, security, calculator, resource, and battery support pages through shared `SeoPathways` research hubs.
- Existing support-page hardening only when it adds concrete scenario coverage.
- New protocol/integration pages only after retest and concrete query evidence.

Retest:

- 2026-09-17 for ranking, CTR, impressions, and internal-link inclusion.

### Batch 4 - Manual Consolidation Review

Scope:

- Possible overlap between similarly targeted Z-Wave pages and any external/legacy duplicated compare-direction pages.

Decision rule:

- Prepare recommendations only. Internal duplicate-direction exposure has been removed at the link/sitemap/static-param layer, but do not change canonical tags, redirects, or schema fields automatically.
- Compare hub schema parity is also recommendation-only until structured-data approval is granted.

## Conflict Checks

- GSC Agent-1/Agent-2 decision said "monitor, not confirmed technical incident"; implementation treats the code/content work as risk reduction and opportunity capture, not proof of causation.
- Technical diagnosis wanted raw HTML links; implementation verified actual production HTML rather than only React component output.
- Content diagnosis wanted security trust correction; implementation reduced unsupported claims instead of adding unverified methodology.
- Architecture diagnosis wanted Silo proof; implementation now includes rendered `<main>` crawl counts rather than relying on source inspection, but those counts are accepted only as crawl-recovery evidence until topic-specific retest data arrives.
- Onsite-growth and website-seo read-only reviews warned that the shared `Research Hubs` links may inflate internal-link counts and flatten sitewide prominence. This is accepted as a residual risk: the links are smart-lock relevant and useful for crawl recovery, but the counts should not be treated as proof of topical authority. If GSC retests underperform, narrow `Research Hubs` by page topic.
- Website-seo final review also flagged visible `/compare` priority links versus unchanged CollectionPage ItemList schema. This remains gated by the protected structured-data rule instead of being changed automatically.
- API cleanup removes the stale dynamic related-articles path so calculator related links have one source of truth.

## Validation Metrics

| Area | Metric | Retest Date |
|---|---|---|
| Weekly GSC monitoring | clicks, impressions, CTR, average position for affected pages/queries | 2026-08-13 and weekly |
| Technical/Silo | raw HTML links, crawl depth, relevant inbound-link counts, admin/API absence from human sitemap | 2026-08-21 and 2026-09-04 |
| Compare pages | query CTR, clicks, average position, snippet alignment for brand-pair queries | 2026-09-03 |
| Door compatibility | clicks from 0 baseline, CTR, position stability, hub inbound-link count | 2026-09-03 |
| Security flagship | recrawl/snippet alignment, CTR, old overclaim absence | 2026-09-03 and 2026-09-17 |
| Best/protocol/integration | impressions, ranking movement, content quality gate completion, Silo support links | 2026-09-17 |
| Protected schema parity | compare hub visible ItemList candidates vs JSON-LD ItemList, schema validation path, approval decision | Manual approval required |

## Latest Local Validation

- `npm run test:seo` passed after adding comparison URL canonicalization checks and the SEO regression script.
- `npx tsx tests/evidence-panels.test.ts` passed after aligning the security article evidence-date assertion with the current `2026-08-06` evidence status.
- `npx tsc --noEmit` passed after replacing loose nullable test fixtures with the actual `Brand` / `ProductWithBrand` shapes.
- `npm run lint` passed.
- `npm run next:build` passed.
- `npm run build` passed.
- Rendered `/sitemap.xml` on `http://localhost:3012` returned 1575 URLs, 1081 compare URLs, and 0 duplicate unordered comparison pairs.
- Rendered `/sitemap.xml` on `http://localhost:3210` returned 1081 compare URLs and 0 duplicate unordered comparison pairs.
- Production checks on `http://localhost:3210` returned 200 for canonical priority compare URLs and 404 for their reverse-direction duplicate URLs.
- Production checks on `http://localhost:3210` verified server-rendered evidence panels for `/calculators/battery-life`, `/calculators/compatibility`, `/calculators/signal-strength`, and `/calculators/lock-tco`.
- Rendered priority compare pages did not link to opposite-direction duplicate URLs for Kwikset/Schlage, Tedee/August, Veise/Schlage, or Lockly/Schlage.
- Static source scan verifies internal `/compare/*-vs-*` links resolve to `getCanonicalComparisonHref()`.
- Follow-up HTTP checks returned 200 for the prior crawl failures: `/brands/defiant`, `/compare/schlage-vs-tedee`, and `/brands/salto/salto-neoxx-g3`.
- `git diff --check` passed.

## Current Exit Status

- Batch 1 is implemented and locally verified.
- Batch 2 compare intent packaging and priority compare Silo links are implemented for first pass.
- Batch 3 best-page evidence boundaries and limited existing protocol/integration support-page hardening are implemented for first pass.
- Core-page >=50 crawl-recovery inbound-link evidence is complete for this batch with rendered `<main>` crawl evidence; topical-authority resolution remains pending retest.
- Broad new protocol/integration expansion and any duplicate-direction consolidation remain planned and gated.
- No backlink work was introduced.
- No automatic canonical, redirect, or schema generator edits were made.
